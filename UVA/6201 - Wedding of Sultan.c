#include <stdio.h>
#include <string.h>
char str[100][100];
char q[1000];
int tail,head;

void push(char item)
{
    q[tail] = item;
    tail++;
}

int main()
{
    int n,i;
    char astr[1000];
    scanf("%d",&n);
    getchar();
    while(n--)
    {
        gets(astr);
        int len = strlen(astr);
        mid = (int)len/2;
        for(i=0;i<len;i++)
        {
            if()
        }
    }
}

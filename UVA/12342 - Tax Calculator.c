#include <stdio.h>

int main()
{
    long long int t,i,n,sum;
    scanf("%lld\n",&t);
    for(i=1;i<=t;i++)
    {
        sum = 0;
        scanf("%lld",&n);
        if(n<=180000)
            sum = 0;
        else if(n>180000)
        {
            if(n<)
        }
    }
}

#include <stdio.h>
#include <string.h>

int main()
{
    int n,group;
    while(scanf("%d %d\n",&n,&group)==2)
    {
        getchar();
        if(n==0)
            break;
        for(i=0;i<)
    }
}
